apiVersion: v1
kind: Service
metadata:
  name: url-shortener-web
  namespace: url-shortener
spec:
  selector:
    app: url-shortener-web
  ports:
    - port: 80
      targetPort: 8000
  type: LoadBalancer
